# M2.2b — Atkinson and Bayer dithering

Extract this overlay into the repository root, then run:

```bash
chmod +x .delivery/M2.2b/APPLY.sh .delivery/M2.2b/VERIFY.sh
./.delivery/M2.2b/APPLY.sh
./.delivery/M2.2b/VERIFY.sh
```
