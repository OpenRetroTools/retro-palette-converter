# Changes

- Added a common `DitherAlgorithm` interface.
- Added registration, lookup, and iteration APIs.
- Migrated `none` and `floyd-steinberg` to independent implementations.
- Made CLI and GUI option lists registry-driven.
- Preserved `DitherMode` and old mapping aliases for compatibility.
- Added registry and CLI integration tests.
