# M2.2a — Dithering Framework

This overlay replaces the monolithic dithering module with a registry-driven package.

Apply after extracting into the repository root:

```bash
chmod +x .delivery/M2.2a/APPLY.sh .delivery/M2.2a/VERIFY.sh
./.delivery/M2.2a/APPLY.sh
./.delivery/M2.2a/VERIFY.sh
```
